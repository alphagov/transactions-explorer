language: python
python:
  - 2.6
  - 2.7

matrix:
  allow_failures:
    - python: 2.6

install: pip install -q -r requirements.txt --use-mirrors

script:
  - nosetests

branches:
  except:
    - release
